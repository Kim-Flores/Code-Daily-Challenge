// Instructions: Just a simple sorting usage. Create a function that returns the elements of the input-array / list in a sorted manner.

//Solution:
sortme = function( names ){
  return names.sort()
}
