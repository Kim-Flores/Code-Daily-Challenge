/*
Instructions:
Now you have to write a function that takes an argument and returns the square of it.
*/

//Solution:
function square(n) {
  return Math.pow(n,2); 
}

//Other Solution:
const square = (n) => n * n;

function square(x) {
  return x ** 2;
}
